
<body>

<div>
    <div class="head row">
        {{#if logged_in}}
        <div>{{id}}</div>
        {{/if}}
    </div>
    {{#each user}}
    <div class="useInfo col">
        <form>
          Favorite Game:  {{favoriteGame}}
        </form>
        <form>
          Location:  {{location}}
        </form>
        <form>
           Bio: {{userBio}}
        </form>
        <li>{{Forum}}</li>
    </div>
    {{/each}}
</div>   
<footer>
    {{!-- <script src="/js/login.js"></script> --}}
    <script src="js/user.js"></script>
</footer> 
</body>
